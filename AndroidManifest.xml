<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="ru.npu3pak.rpg"
          android:versionCode="1"
          android:versionName="1.0">

    <uses-sdk
            android:minSdkVersion="8"
            android:targetSdkVersion="20"/>
    <application
            android:label="@string/app_name"
            android:icon="@drawable/ic_launcher"
            android:theme="@android:style/Theme.NoTitleBar">
        <activity
                android:name=".activities.AdventureActivity"
                android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activities.BattleActivity"/>
        <activity android:name=".activities.TradeActivity"/>
        <activity android:name=".activities.TalkingActivity"/>
        <activity android:name=".activities.InventoryActivity"/>
        <activity android:name=".activities.StatsActivity"/>
    </application>
</manifest>
